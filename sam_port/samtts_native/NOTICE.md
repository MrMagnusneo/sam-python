This directory contains a vendored Python SAM implementation adapted for this repository.

Base source: `samtts` (Python package), version 0.2.0.
Original project summary: Python port of Software Automatic Mouth (SAM).

The code is embedded locally to keep this repository self-contained and to avoid runtime dependency on external packages.
